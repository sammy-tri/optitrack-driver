// This message is derived from the sForcePlateData message
// contained in the NetNat SDK version 4.0

package optitrack;

struct optitrack_forceplate_t {
  int32_t id;                        // Forceplate identifier
  float force[3];
  float moment[3];
}
