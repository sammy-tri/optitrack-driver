// This message is derived from the sRigidBodyData message
// contained in the NetNat SDK version 2.10.0

package optitrack;

struct optitrack_marker_set_description_t {
  string name;
  int32_t num_markers;
  string marker_names[num_markers];
}